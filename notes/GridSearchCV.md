# GridSearchCV

GridSearchCV automatically finds the best hyperparameters for a machine learning model.

It tries many different parameter combinations.

Cross Validation is used for each combination.

Finally, it selects the model with the highest validation accuracy.

---

## Advantages

- Automatic hyperparameter tuning
- Better model performance
- More reliable evaluation
